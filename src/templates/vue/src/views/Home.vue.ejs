<% if (features.includes('router')) { %>
<template>
  <div :class="containerClasses">
    <h1 :class="titleClasses">欢迎使用 <%= projectName %></h1>
    <p :class="descriptionClasses">
      这是一个使用 Vue 3 + <%= buildTool %> 构建的现代化应用
    </p>
    
<% if (features.includes('state-management')) { %>
    <!-- 计数器组件 -->
    <Counter />
<% } %>
    
    <div :class="featuresClasses">
      <h2 :class="featuresTitleClasses">项目特性</h2>
      <div :class="featuresGridClasses">
        <div :class="featureCardClasses">
          <h3 :class="featureCardTitleClasses">Vue 3</h3>
          <p :class="featureCardDescClasses">使用最新的 Vue 3 Composition API</p>
        </div>
        <div :class="featureCardClasses">
          <h3 :class="featureCardTitleClasses">TypeScript</h3>
          <p :class="featureCardDescClasses">完整的 TypeScript 类型支持</p>
        </div>
        <div :class="featureCardClasses">
          <h3 :class="featureCardTitleClasses"><%= buildTool %></h3>
          <p :class="featureCardDescClasses">快速的构建工具</p>
        </div>
<% if (features.includes('tailwind')) { %>
        <div :class="featureCardClasses">
          <h3 :class="featureCardTitleClasses">Tailwind CSS</h3>
          <p :class="featureCardDescClasses">实用优先的 CSS 框架</p>
        </div>
<% } %>
<% if (features.includes('state-management')) { %>
        <div :class="featureCardClasses">
          <h3 :class="featureCardTitleClasses">Pinia</h3>
          <p :class="featureCardDescClasses">Vue 的状态管理库</p>
        </div>
<% } %>
<% if (features.includes('testing')) { %>
        <div :class="featureCardClasses">
          <h3 :class="featureCardTitleClasses">Vitest</h3>
          <p :class="featureCardDescClasses">快速的单元测试框架</p>
        </div>
<% } %>
      </div>
    </div>
  </div>
</template>

<script setup lang="ts">
import { computed } from 'vue'
<% if (features.includes('state-management')) { %>
import Counter from '../components/Counter.vue'
<% } %>

// 样式类计算属性
const containerClasses = computed(() => 
<% if (features.includes('tailwind')) { %>
  'text-center'
<% } else { %>
  'container'
<% } %>
)

const titleClasses = computed(() => 
<% if (features.includes('tailwind')) { %>
  'text-4xl font-bold text-gray-900 mb-4'
<% } else { %>
  'title'
<% } %>
)

const descriptionClasses = computed(() => 
<% if (features.includes('tailwind')) { %>
  'text-lg text-gray-600 mb-8'
<% } else { %>
  'description'
<% } %>
)

const featuresClasses = computed(() => 
<% if (features.includes('tailwind')) { %>
  'mt-12'
<% } else { %>
  'features'
<% } %>
)

const featuresTitleClasses = computed(() => 
<% if (features.includes('tailwind')) { %>
  'text-2xl font-semibold text-gray-900 mb-6'
<% } else { %>
  'features-title'
<% } %>
)

const featuresGridClasses = computed(() => 
<% if (features.includes('tailwind')) { %>
  'grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6'
<% } else { %>
  'features-grid'
<% } %>
)

const featureCardClasses = computed(() => 
<% if (features.includes('tailwind')) { %>
  'bg-white p-6 rounded-lg shadow-md'
<% } else { %>
  'feature-card'
<% } %>
)

const featureCardTitleClasses = computed(() => 
<% if (features.includes('tailwind')) { %>
  'text-lg font-semibold text-gray-900 mb-2'
<% } else { %>
  'feature-card-title'
<% } %>
)

const featureCardDescClasses = computed(() => 
<% if (features.includes('tailwind')) { %>
  'text-gray-600'
<% } else { %>
  'feature-card-desc'
<% } %>
)
</script>

<% if (!features.includes('tailwind')) { %>
<style scoped>
.container {
  text-align: center;
}

.title {
  font-size: 2.5rem;
  font-weight: bold;
  color: #111827;
  margin-bottom: 1rem;
}

.description {
  font-size: 1.125rem;
  color: #6b7280;
  margin-bottom: 2rem;
}

.features {
  margin-top: 3rem;
}

.features-title {
  font-size: 1.5rem;
  font-weight: 600;
  color: #111827;
  margin-bottom: 1.5rem;
}

.features-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
  gap: 1.5rem;
}

.feature-card {
  background: white;
  padding: 1.5rem;
  border-radius: 0.5rem;
  box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
}

.feature-card-title {
  font-size: 1.125rem;
  font-weight: 600;
  color: #111827;
  margin-bottom: 0.5rem;
}

.feature-card-desc {
  color: #6b7280;
}
</style>
<% } %>
<% } else { %>
<!-- 路由功能未启用时的占位文件 -->
<template>
  <div>Home View</div>
</template>

<script setup lang="