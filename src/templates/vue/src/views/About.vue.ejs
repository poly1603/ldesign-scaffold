<% if (features.includes('router')) { %>
<template>
  <div :class="containerClasses">
    <h1 :class="titleClasses">关于我们</h1>
    <div :class="contentClasses">
      <p :class="paragraphClasses">
        这是一个使用 Vue 3 + TypeScript + <%= buildTool %> 构建的现代化 Web 应用程序。
      </p>
      <p :class="paragraphClasses">
        我们致力于提供最佳的开发体验和用户体验。
      </p>
      
      <div :class="infoGridClasses">
        <div :class="infoCardClasses">
          <h3 :class="infoTitleClasses">技术栈</h3>
          <ul :class="listClasses">
            <li>Vue 3</li>
            <li>TypeScript</li>
            <li><%= buildTool %></li>
<% if (features.includes('tailwind')) { %>
            <li>Tailwind CSS</li>
<% } %>
<% if (features.includes('state-management')) { %>
            <li>Pinia</li>
<% } %>
<% if (features.includes('router')) { %>
            <li>Vue Router</li>
<% } %>
<% if (features.includes('testing')) { %>
            <li>Vitest</li>
<% } %>
          </ul>
        </div>
        
        <div :class="infoCardClasses">
          <h3 :class="infoTitleClasses">特性</h3>
          <ul :class="listClasses">
            <li>响应式设计</li>
            <li>组件化架构</li>
            <li>类型安全</li>
            <li>热重载</li>
<% if (features.includes('testing')) { %>
            <li>单元测试</li>
<% } %>
<% if (features.includes('prettier')) { %>
            <li>代码格式化</li>
<% } %>
          </ul>
        </div>
      </div>
      
      <div :class="contactClasses">
        <h3 :class="contactTitleClasses">联系我们</h3>
        <p :class="contactTextClasses">
          如果您有任何问题或建议，请随时联系我们。
        </p>
        <div :class="contactLinksClasses">
          <a
            href="mailto:contact@example.com"
            :class="contactLinkClasses"
          >
            发送邮件
          </a>
          <a
            href="https://github.com"
            target="_blank"
            rel="noopener noreferrer"
            :class="contactLinkClasses"
          >
            GitHub
          </a>
        </div>
      </div>
    </div>
  </div>
</template>

<script setup lang="ts">
import { computed } from 'vue'

// 样式类计算属性
const containerClasses = computed(() => 
<% if (features.includes('tailwind')) { %>
  'max-w-4xl mx-auto'
<% } else { %>
  'container'
<% } %>
)

const titleClasses = computed(() => 
<% if (features.includes('tailwind')) { %>
  'text-4xl font-bold text-gray-900 mb-8 text-center'
<% } else { %>
  'title'
<% } %>
)

const contentClasses = computed(() => 
<% if (features.includes('tailwind')) { %>
  'prose prose-lg max-w-none'
<% } else { %>
  'content'
<% } %>
)

const paragraphClasses = computed(() => 
<% if (features.includes('tailwind')) { %>
  'text-gray-600 mb-6 leading-relaxed'
<% } else { %>
  'paragraph'
<% } %>
)

const infoGridClasses = computed(() => 
<% if (features.includes('tailwind')) { %>
  'grid md:grid-cols-2 gap-8 my-12'
<% } else { %>
  'info-grid'
<% } %>
)

const infoCardClasses = computed(() => 
<% if (features.includes('tailwind')) { %>
  'bg-white p-6 rounded-lg shadow-md'
<% } else { %>
  'info-card'
<% } %>
)

const infoTitleClasses = computed(() => 
<% if (features.includes('tailwind')) { %>
  'text-xl font-semibold text-gray-900 mb-4'
<% } else { %>
  'info-title'
<% } %>
)

const listClasses = computed(() => 
<% if (features.includes('tailwind')) { %>
  'space-y-2 text-gray-600'
<% } else { %>
  'list'
<% } %>
)

const contactClasses = computed(() => 
<% if (features.includes('tailwind')) { %>
  'text-center mt-12 p-8 bg-gray-50 rounded-lg'
<% } else { %>
  'contact'
<% } %>
)

const contactTitleClasses = computed(() => 
<% if (features.includes('tailwind')) { %>
  'text-2xl font-semibold text-gray-900 mb-4'
<% } else { %>
  'contact-title'
<% } %>
)

const contactTextClasses = computed(() => 
<% if (features.includes('tailwind')) { %>
  'text-gray-600 mb-6'
<% } else { %>
  'contact-text'
<% } %>
)

const contactLinksClasses = computed(() => 
<% if (features.includes('tailwind')) { %>
  'flex justify-center space-x-6'
<% } else { %>
  'contact-links'
<% } %>
)

const contactLinkClasses = computed(() => 
<% if (features.includes('tailwind')) { %>
  'text-blue-600 hover:text-blue-800 transition-colors font-medium'
<% } else { %>
  'contact-link'
<% } %>
)
</script>

<% if (!features.includes('tailwind')) { %>
<style scoped>
.container {
  max-width: 800px;
  margin: 0 auto;
}

.title {
  font-size: 2.5rem;
  font-weight: bold;
  color: #111827;
  margin-bottom: 2rem;
  text-align: center;
}

.content {
  line-height: 1.7;
}

.paragraph {
  color: #6b7280;
  margin-bottom: 1.5rem;
  font-size: 1.125rem;
}

.info-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
  gap: 2rem;
  margin: 3rem 0;
}

.info-card {
  background: white;
  padding: 1.5rem;
  border-radius: 0.5rem;
  box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
}

.info-title {
  font-size: 1.25rem;
  font-weight: 600;
  color: #111827;
  margin-bottom: 1rem;
}

.list {
  list-style: none;
  padding: 0;
}

.list li {
  color: #6b7280;
  margin-bottom: 0.5rem;
  padding-left: 1rem;
  position: relative;
}

.list li::before {
  content: '•';
  color: #3b82f6;
  position: absolute;
  left: 0;
}

.contact {
  text-align: center;
  margin-top: 3rem;
  padding: 2rem;
  background: #f9fafb;
  border-radius: 0.5rem;
}

.contact-title {
  font-size: 1.5rem;
  font-weight: 600;
  color: #111827;
  margin-bottom: 1rem;
}

.contact-text {
  color: #6b7280;
  margin-bottom: 1.5rem;
}

.contact-links {
  display: flex;
  justify-content: center;
  gap: 1.5rem;
}

.contact-link {
  color: #2563eb;
  text-decoration: none;
  font-weight: 500;
  transition: color 0.2s;
}

.contact-link:hover {
  color: #1d4ed8;
}
</style>
<% } %>
<% } else { %>
<!-- 路由功能未启用时的占位文件 -->
<template>
  <div>About View</div>
</template>

<script setup lang="ts">
// 这是一个占位组件
</script>
<% } %>