# 依赖
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# 构建输出
dist/
build/
out/
.next/
.nuxt/
.output/
.vite/

# 环境变量
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# 日志
logs/
*.log

# 运行时数据
pids/
*.pid
*.seed
*.pid.lock

# 覆盖率目录
coverage/
*.lcov
.nyc_output/

# 测试
.jest/

# 缓存
.cache/
.parcel-cache/
.eslintcache
.stylelintcache

# 编辑器
.vscode/
.idea/
*.swp
*.swo
*~

# 操作系统
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# 临时文件
*.tmp
*.temp

<% if (projectType === 'react' || projectType === 'vue') { %>
# 前端特定
.eslintcache
storybook-static/
<% } %>

<% if (projectType === 'node') { %>
# Node.js 特定
.npm
.node_repl_history
*.tgz
.yarn-integrity

# 数据库
*.db
*.sqlite
*.sqlite3

# 上传文件
uploads/

# PM2
.pm2/
<% } %>

<% if (features && features.includes('database')) { %>
# 数据库
*.db
*.sqlite
*.sqlite3
migrations/
<% } %>

<% if (features && features.includes('docker')) { %>
# Docker
.dockerignore
<% } %>

<% if (projectType === 'library') { %>
# 库特定
*.tgz
package-lock.json
yarn.lock
pnpm-lock.yaml
<% } %>

# 其他
.turbo/
.vercel/
.netlify/